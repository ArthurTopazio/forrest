/* import treeVector from './treeLabel.svg';
import englandFlag from './englandFlag.svg';
import finnlandFlag from './finnlandFlag.svg'; */

import mockTreePic, { engFlag, finnFlag } from 'consts/mockData/mockData';

const vectorCollection = {
  tree: mockTreePic,
  englandFlag: engFlag,
  finnlandFlag: finnFlag,
};

export default vectorCollection;
